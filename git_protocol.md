reminder of git protocol
=========================

to update your folder and sync it to github repo
------------------------------------------------

    git pull origin master


when you want to push something
--------------------------------

    git add path/to/filename

    git commit -m 'what changed'

    git push origin master

